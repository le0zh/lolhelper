<phone:PhoneApplicationPage
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    xmlns:edc="clr-namespace:Microsoft.Expression.Controls;assembly=Microsoft.Expression.Drawing"
    x:Class="LolWikiApp.AboutPage"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True"
    shell:SystemTray.Opacity="0"
    shell:SystemTray.BackgroundColor="White"
    shell:SystemTray.ForegroundColor="Black"
    Style="{StaticResource TransitionPageStyle}">
    
    <!--LayoutRoot is the root grid where all page content is placed-->
	<Grid x:Name="LayoutRoot" Background="Transparent">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!--TitlePanel contains the name of the application and page title-->
		<StackPanel Grid.Row="0" Margin="12,17,0,28">
			<Border BorderThickness="15 0 0 0" Margin="0 -12 0 8" BorderBrush="{StaticResource PhoneAccentBrush}">
				<TextBlock Text="英雄联盟助手" Margin="12 0 0 0" VerticalAlignment="Center"
					FontSize="{StaticResource PhoneFontSizeLarge}" />
			</Border>
            
			<TextBlock  Margin="0 12 0 -12" Text="关于" FontSize="{StaticResource PhoneFontSizeExtraLarge}"/>
            
		</StackPanel>

		<StackPanel Grid.Row="1" Margin="12,0,12,0" >
            
			<TextBlock Margin="0 8 0 0">
				<Run Text="软件版本： v1.0"/>
			</TextBlock>

			<TextBlock Margin="0 8 0 0">
				<Run Text="官方数据版本："/>
				<Run Text="{Binding DataVersion}"/>
			</TextBlock>

			<TextBlock Margin="0 8 0 0">
				<Run Text="更新日期："/>
				<Run Text="{Binding DataLastUpdated}"/>
			</TextBlock>
            
			<ListBox Height="50">
				
                
            </ListBox>
		</StackPanel>

      
	</Grid>

</phone:PhoneApplicationPage>